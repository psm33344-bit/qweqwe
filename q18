#include <stdio.h>

void print_max(int[]);

int main(){
    int arry[7]={4, 5, 8, 1, 2, 3, 7};

    print_max(arry);

    return 0;
}
void print_max(int *pa){
    int max=0;
    for(int i=0; i<7; i++){
        if(max < pa[i])
        max= pa[i];
    }
    printf("가장 큰 값은:%d", max);
}
